import React from "react";

// Dummy implementation of Bluetooth Printer integration
export const BluetoothPrinter = {
  printReceipt: (text: string) => {
    // Placeholder for actual Bluetooth print logic
    console.log(`Sending to printer: ${text}`);
  },
};
